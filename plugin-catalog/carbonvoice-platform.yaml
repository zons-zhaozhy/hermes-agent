name: carbonvoice-platform
repo: https://github.com/PhononX/hermes-plugin-carbonvoice
sha: 55867dba9c6645ce493fd955970302e59a4548ea
description: Carbon Voice gateway adapter for Hermes Agent. Connects via Socket.IO (primary) with REST
  polling fallback — no public webhook or tunnel required. Persists a cursor to disk so messages received
  while Hermes is offline are processed on the next startup. Replies via POST /v3/messages/start, so the
  Her
maintainer: PhononX
tier: community
category: platform
docs_url: https://github.com/PhononX/hermes-plugin-carbonvoice#readme
capabilities:
  provides_tools: []
  provides_hooks: []
  provides_middleware: []
  requires_env:
  - name
