name: bridge-adapter
repo: https://github.com/Kesuek/hermes-bridge-adapter
sha: 406a53d75cd68378067dddc7273d4261c0155fd5
description: Generic JSON-file-based bridge adapter. Bridges self-register via a registry/ manifest (presence
  = registered, rm = unregistered). Polls inbox/ for incoming messages, writes outgoing to outbox/. External
  services (imsg, Matrix, Telegram, ...) write/read JSON files in a shared directory.
maintainer: Kesuek
tier: community
category: platform
docs_url: https://github.com/Kesuek/hermes-bridge-adapter#readme
capabilities:
  provides_tools: []
  provides_hooks: []
  provides_middleware: []
  requires_env:
  - name
