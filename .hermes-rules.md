# Hermes Agent 仓库常驻规则

## 预调查门禁（最高优先级，违反即停止）
任何 write_file / patch / execute_code(写入) / terminal(写入) 之前必须完成五查，未完成=不许动笔：
- ① 读代码：read_file 目标文件磁盘现状（改的不是记忆中的版本）
- ② 追链路：代码分析/导航/影响评估第一条调用走 gitnexus_query / codegraph_context，grep 只作兜底，不手动 grep 顶替结构化工具
- ③ 查知识库：相关 skill 先 skill_view 加载；内容为 [SKILL_PRUNED] 须等重载
- ④ 搜同类：search_files 全库搜同类文件/函数/模式，确认不是重复造轮子
- ⑤ 查规范：coding-conventions / naming-conventions / 项目 AGENTS.md 已读
禁止：凭记忆写代码、不读文件就 patch、不查调用链就改接口、不搜同类就新建文件、跳过测绘直接输出方案、用"应该/大概/一般来说"替代实测验证。

## 禁令总纲（唯一权威版本，五组 35 条）
以下为本仓库全部行为禁令的体系化总纲。散落在 memory/画像/历史会话中的旧表述与本段冲突时，以本段为准。

### A. 行为五禁（最高优先级，任何场景通用）
- A1 禁绕过：护栏/拦截/报错禁换通道硬闯或丢回用户手工；被拦须读拦截信息补证据原路重试
- A2 禁瞎猜：参数/表名/列名/路由/签名禁凭记忆猜测，必须读源码或 schema 实证；查不到就明说[未查证]禁编造
- A3 禁静默：性能/行为键禁静默缺省；两态键缺省必 WARNING；解析失败必须响亮报错禁伪装正常
- A4 禁吞异常：except 必须 logger 或 raise，禁 2>/dev/null 吞错；截断处禁补写，先取完整日志再定性
- A5 禁兜底：零 fallback，报错原文不遮盖；禁拿换字面量/降级路径掩盖病根（病根=硬编码时须拆配置根治）

### B. 诊断纪律
- B1 错误信息本身往往就是答案：no such host=DNS，401=认证缺失，exit≠0=执行失败——先读原文提取文件名:行号
- B2 禁穷举试探/逐个 curl/猜配置/绕弯路；422 读 detail.loc，404 先对照路由清单
- B3 DB 禁猜表名列名，先 \dt/\d 查 schema；查询返回空≠没数据，先确认拼写
- B4 连续 2 次相同失败即切换策略，禁第 3 次重试
- B5 禁日志编造/脑补（exit 1 禁编成正常 kill）；禁截断诊断输出（截断=埋坑）；大日志先 grep 根因行再读

### C. 编码纪律
- C1 禁硬编码：表名/字段/SQL/IP/密钥/路径/监管条款字面量，一律走配置/env；raise 新异常必须带 from e
- C2 零正则铁律：解析类优先 str 方法（split/find/strip/endswith），豁免须 # re-ok+理由
- C3 改文件一律 patch/write_file，禁 cat>>/heredoc/临时脚本批量 replace；写前必读磁盘现状
- C4 新建文件/函数前全库查重禁重复造轮子；新函数落笔带 Contract 段或 assert
- C5 测试：一律 scripts/run_tests.sh 禁裸 pytest；期望值独立推导禁凑绿灯；修 bug 先在旧代码跑红再修绿；禁 change-detector 测试/测试读源码文本

### D. 工程纪律
- D1 先体系后问题（磨刀不误砍柴工）：同类问题第二次出现且无可复用工具、手工操作重复≥2 次未沉淀、纠正未落 rules/guard——须沉淀后同轮用体系解当前问题；边界=禁为体系而体系/借体系搁置任务，一次性问题直接修
- D2 禁打地鼠：同类缺陷全局排查一次修完，不止修单点
- D3 禁留尾巴：顺带发现的真缺陷当场修完禁标[未修]/「后续迭代」；零「UI未接」「链在跑」式拖延收尾
- D4 禁请示式收尾：可控可回滚的自行决断办完报结果；仅真花钱/对外承诺/不可逆伤核心资产才请拍板，且必附建议+依据
- D5 禁只口头收尾：「处理好」=真做完，[实测]必须当轮贴原始输出；完成声明=每条验收标准有工具输出背书
- D6 凡有改动即 commit+push 零积压；禁把内核缺陷绕进扩展避难所；禁绕开 OntoX 底座重复造轮子

### E. 业务/表达红线
- E1 对外材料绝对禁提「v7/复刻/遗留参照/银丰」及 T1D_* 表名——只写「N个场景实战沉淀」等纯能力表述
- E2 交付文档禁英文缩写须中文全称；禁自造比喻/生僻词；大白话
- E3 监管要求禁摘要：法规全文逐字入库，引用条文号必核版本链，二手转述互斥是常态
- E4 AML 不漏报优先于误报：首要=不漏报（漏制裁=合规事故），误报走人工复核；大额=四标准客观命中即法定报送，禁加审批
- E5 语义分类禁关键词正则猜意图，交 LLM 按自含适用条件规则判；来源表契约禁 agent 单方面切换，须证据对比表请拍板

## 工作方式总纲
- 先体系后问题，触发条件（命中其一）：同类问题第二次出现且无可复用工具/规范/护栏；手工操作重复≥2次未沉淀为脚本/skill；纠正只留会话或memory、未落 rules/AGENTS/guard
- 触发后：解法沉淀到对应层——操作→脚本/skill；纪律→.hermes-rules.md+AGENTS.md；强制力→guard 插件；同轮用体系解当前问题
- 边界：体系升级必须服务当前问题且同轮完成；禁为体系而体系、禁借体系之名搁置当前任务；一次性问题直接修，禁过度工程

## 单一事实源纪律（防多真相漂移）
- 同一规则只允许一处实现：token/字段归一化、配置与目录解析、并发上限、阈值常量、数值归一化、对外载荷形状——其余位置一律 import 使用，禁各写一份
- 收敛必须配机器门禁：AST 结构化解析（禁正则文本匹配）声明「语义 → 唯一 owner」，其余文件出现同名定义即 FAIL；门禁必带 --self-test 负例（人为注入漂移必须被抓，否则门禁只是装饰）
- 一处漂移被修 = 同类全局排查（先测绘范围全部定义面，再逐条收敛），禁只修点名位置
- 实测基线：真树注入漂移 → 门禁 FAIL rc=1 且报 file:line；还原后 diff 干净——正例负例都要跑

## 编辑纪律
- 改文件一律 patch/write_file，禁 cat>>/heredoc/临时脚本批量 replace
- 写前必读目标文件磁盘现状；同文件 patch 连败 2 次改 write_file 整写
- 新函数落笔带 Contract 段或 assert；新建文件前全库查重

## 诊断纪律
- 错误信息本身往往就是答案：no such host=DNS，401=认证缺失，exit≠0=执行失败
- 遇报错先读日志堆栈提取文件名:行号，禁穷举试探/逐个 curl/猜配置
- 连续 2 次相同失败即切换策略，禁第 3 次重试

## 测试纪律
- 一律 scripts/run_tests.sh，禁裸 pytest
- 迭代期窄集秒级，全量仅收尾
- 期望值独立推导，禁先写代码再凑绿灯
- 修 bug 前先写测试在旧代码上跑红（red on base）——证明测试真能抓住该缺陷，再修绿

## 汇报纪律
- 称[实测]必须当轮贴原始输出进回复正文
- 长任务主动汇报阶段进展，禁干等
- 完成声明=每条验收标准有真实工具输出背书
- commit message 四段式：现象→根因→修法→前后数字对照表；能量化的必须给数字（修前 X→修后 Y）

## 健康度自审
- 持续开发期的健康信号=删行≥增行；连续提交只增不删=在堆代码，回头收缩

## 上游同步纪律（防合并代价滚雪球）
- 根因量化（0923 实测）：本地 718 提交改 1103 文件，其中 846 个落在上游同期活跃文件上——冲突成本 ∝ 重叠面，与上游周均数千提交的节奏相乘
- 新定制默认落插件层（plugins/）/skill 层，禁再直接改 cli.py、agent/*.py、tools/*.py 核心文件——窄腰原则，与上游架构方向一致（能力在边缘生长）；hook 覆盖不了的先申明再动核心
- 已在核心文件里的本地改动（重灾区：cli.py×26、goals.py×21、tool_executor.py×21、conversation_loop.py×16、agent_init.py×16），趁每次同步逐步外迁；外迁时用「插件 hook + 等价实测」对照，行为零损失才删核心补丁
- 同步频率前移：小步多频（周级）优于大步低频（月级堆积 4512 提交）；同步前先跑 git log upstream/main..HEAD --name-only 测重叠面，重叠面 >300 文件时必须分批
- 修测试先验存量：合并后发现测试失败，先在 merge 前代码（worktree + 软链 node_modules）跑对照——实测 14 个 UI 失败是 merge 前就坏（旧 SkillCatalog 测试没跟上 iframe 重构），禁把存量坏账算到合并头上也禁顺手忽略

## 收尾纪律
- 顺带发现的真缺陷当场修完禁标[未修]留后续
- 凡有改动即 commit+push 零积压

## 反方审查纪律（DevilAdvocateAudit）
- 触发：重大决策/方案定稿信号（选型、契约拍板、定稿、收尾声明）且本会话尚无反方视角
- 动作：delegate_task 委派只找漏洞的唱反调子代理（独立上下文、只读不写）；结论逐条定性，真缺陷当场修
- 铁律：决策与审查分脑，单视角定稿=未审计；委派失败=重派，禁自审替代；过渡性自审须明标「非独立审查」
- 唯一旁路：用户明示豁免

## 范式演化纪律（SystemOne，2026-09-23）
- 设计任何管线/步骤前先问：这步是判断题（分类/打分/二值）还是作文题（生成自由文本）？判断题禁用大模型生成再解析——用小型判定件（规则/本地小模型/校准判定 API）
- 判定件三验收：目标语言实测准确率、校准质量（ECE/同标签置信度摆幅）、冷热延迟分层——只测 accuracy 不够
- 出厂未校准的判定模型（置信度两极摆）先拟合温度再评估能力，禁把校准问题误判为能力问题
- AI 生态出现新范式物种（如 Jev 类 System One 判定模型）必须深学内化：研究→实测→沉淀报告→更新 skill，禁只留会话印象
