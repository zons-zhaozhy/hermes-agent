/** Neutral, unfinished state shared by pending tasks and goal criteria. */
export function StatusPendingIcon() {
  return (
    <span
      aria-hidden
      className="box-border size-[0.7rem] rounded-full border border-dashed border-muted-foreground/60"
    />
  )
}
